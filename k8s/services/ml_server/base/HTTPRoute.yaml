apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ml-server-route
spec:
  parentRefs:
  - name: ml-server-gw
  hostnames:
  - ml-server-production.com
  rules:
  - matches:
      - path:
          type: PathPrefix
          value: /api/
      - path:
          type: PathPrefix
          value: /auth/
    backendRefs:
    - name: ml-server-svc
      port: 8000
