apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: ml-server-gw
  namespace: ml-server-dev
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 8000
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: Same