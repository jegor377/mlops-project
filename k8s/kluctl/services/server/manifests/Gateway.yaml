apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: ml-server-gw
  namespace: mlops-project
spec:
  gatewayClassName: traefik
  listeners:
  - protocol: HTTP
    port: 80
    name: http
    allowedRoutes:
      namespaces:
        from: Same
