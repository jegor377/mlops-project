apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: ml-server-gw
  namespace: mlops-project
spec:
  gatewayClassName: traefik
  listeners:
  - protocol: HTTP
    port: 8000
    name: web
    allowedRoutes:
      namespaces:
        from: Same
